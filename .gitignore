# 真实公开模板不进仓库。
#
# 发布机关（各地人社局、英国 ACAS）虽然写着"免费提供、供参照使用"，
# 但页脚仍保留版权声明，属于灰区；ACAS 那份的许可我们没有逐条核实过。
# git 历史是删不掉的——推上去之后 git rm 也没用，文件仍留在提交历史里。
# 所以宁可不推。每份的 URL 和已知的许可情况见 样本来源.md，可以自己下载到这个目录。
模板/

# 抓取的中间产物
*.doc
*.wps

__pycache__/
*.pyc
.streamlit/
.venv/
venv/
