# 样本来源登记

这个项目用两类样本，作用不同，**是成对的对照组，不是凑数量**：

| 类型 | 作用 | 在不在仓库里 |
|---|---|---|
| **真实公开模板** | 阴性对照——验证工具能正确报出「这份合同没约定 IP 归属」 | ❌ 不在（版权灰区，见下） |
| **模拟样本** | 阳性对照 + 违法样本——官方模板的金额和期限**全是留白**，数字规则在上面一条都测不出来 | ✅ 在 |

---

## 一、模拟样本（在仓库里）

由 `make_sample.py` 生成，**不是真实合同**。三份构成一个测试矩阵：

| 文件 | 设计意图 | 期望输出 |
|---|---|---|
| `模拟-合规样本.docx` | 3 年期 + 3 个月试用期；条款齐全；薪资放在表格里 | 数字规则全合规；四项条款全部「已检索到」 |
| `模拟-违法样本.docx` | 1 年期 + **6 个月**试用期；竞业 **3 年**；违约金约定在"提前离职"情形 | 三条数字规则**全部报违法** |
| `模拟-缺条款样本.docx` | 只有法定必备项，无 IP／交接／竞业 | 竞业与违约金报「信息不足」（**不是「合规」**）；四项条款「未检索到」 |

### 模拟数据的原则

一律自证是假的，避免撞上任何真实主体：

- 公司名 `示例科技（测试）有限公司`，人名 `张测试`
- 手机用官方保留号段 `13800000000`
- 金额取非整数（`8,765` / `6,540` / `7,890`），避开 `10000` 这类可能撞真实数据的圆整数
- 每份文档**正文第一段**带 `【测试用模拟文本 · 非真实合同】` 标注
  （放正文不放页眉——`python-docx` 默认读不到页眉，转格式时也容易丢）

### ⚠️ 标注措辞踩过一次坑

初版标注里写了「其中知识产权归属条款依据《专利法》第六条拟制」，
结果工具在一份**根本没有 IP 条款**的样本上报出「知识产权归属：已检索到」——
命中的正是这句标注本身。**测试样本污染了测试结果。**

所以标注里不再出现任何检查项的名字，拟制依据挪到这份文件里（这份不进检测）。

### 拟制依据

`模拟-合规样本.docx` 的知识产权归属条款，依据
《中华人民共和国专利法》第六条、《中华人民共和国著作权法》第十八条的职务成果规则拟制。
它不是从任何真实合同抄来的，也不构成范本。

---

## 二、真实公开模板（不在仓库里，自己下）

放进 `模板/` 目录即可被 `scan_all.py` 读到。

| 文件名 | 来源 | URL |
|---|---|---|
| `宿州-劳动合同通用.docx` | 宿州市人力资源和社会保障局 | https://rsj.ahsz.gov.cn/xzzq/195506471.html |
| `宿州-劳务派遣.docx` | 同上 | 同上 |
| `宿州-快递从业人员.docx` | 同上 | 同上 |
| `武汉-简易劳动合同.docx` | 武汉市人力资源和社会保障局 | https://rsj.wuhan.gov.cn/bsfw_17/bgxz/202007/t20200721_1407331.html |
| `武汉-非全日制劳动合同.docx` | 同上 | 同上 |
| `UK-ACAS-employee.docx` | 英国 ACAS，written statement of employment particulars | https://www.acas.org.uk/templates-for-written-statements |

宿州那三份原文是 `.doc`，需要用 Word 另存为 `.docx` 才能被 `python-docx` 读取。

### 为什么不进仓库

发布机关写着"免费提供、供参照使用"，但页面页脚仍保留版权声明。
**我们没有逐条核实过每一份的许可条款**，而 git 历史删不掉——推上去之后再 `git rm` 也无济于事。
所以选择不推，只登记 URL。

---

## 三、样本独立性：为什么「6 份」这个数不能直接写

用两个指标量过手上这 6 份（字符 8-gram 相似度）：

| 组别 | 相似度 |
|---|---|
| 宿州 3 份两两之间 | 0.205 – **0.455** |
| 武汉 2 份之间 | **0.127** |
| **跨发布机关** | 0.010 – **0.018** |

组内最低 0.127，组外最高 0.018——**差 7 倍，中间空着一整个数量级**。

宿州那 3 份的工作交接条款，条号不同（第十七条／第二十三条／第十六条）但正文一字不差，
是同一套模板的变体。

**结论：宿州 3 份压成 1 份，武汉 2 份算 1.5 份，「6 份」的真实等效量约 3.5 份独立样本。**

所以 README 里写的是"6 份文件、等效约 3.5 份独立样本"，不是"在 6 份模板上测试"。
